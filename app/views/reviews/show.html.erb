<%= render "shared/header" %>

<div class="container">
  <table class="table table-striped">
   <%= link_to '編集', edit_review_path(@review.id), method: :get, class:"btn btn-outline-primary" %>
   <%= link_to '削除', review_path(@review.id), method: :delete, class:"btn btn-outline-primary" %>
   
  </table>
</div>